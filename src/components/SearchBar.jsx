function SearchBar() {
  return (
    <input
      type="text"
      placeholder="Search Notifications..."
      style={{
        width: "100%",
        padding: "10px",
        marginBottom: "10px",
      }}
    />
  );
}

export default SearchBar;